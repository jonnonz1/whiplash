# Google Analytics 4 measurement ID. Analytics only load when this is set,
# so local dev and preview builds stay clean. Copy to .env (gitignored) for
# local testing, and add it as a service variable on Railway for production:
#   VITE_GA_ID=G-XXXXXXXXXX
VITE_GA_ID=

# Google Analytics 4 measurement ID. Production already has a baked-in default
# (see src/lib/analytics.js), so you only need this to OVERRIDE it — e.g. point
# a local/preview build at a different GA property. Dev stays clean when unset.
#   VITE_GA_ID=G-XXXXXXXXXX
VITE_GA_ID=
